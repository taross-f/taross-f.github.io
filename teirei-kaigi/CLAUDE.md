# 定例会議 (Teirei Kaigi)

8番出口インスパイアの「ビデオ会議異変探し」ゲーム。claude.ai上でプロトタイピングしたものをこのリポジトリに移管した。

## コンセプトとルール

- 毎日の日次定例会議(Zoom風UI)に出席する。月〜金の5日制。
- **異変を感じたら、即「退出」ボタン。異変がなければ、35秒の会議に最後まで残る。**
- 判定: 異変あり+退出=正解 / 異変なし+完走=正解 / それ以外=失敗で月曜に戻る。
- 8番出口の「進む/引き返す」の二択を「退出する/残る」に写像している。ボタンは退出ひとつだけ。

## アーキテクチャ

- `src/TeireiKaigi.jsx` に全実装(単一コンポーネント、依存はReactのみ)。
- `src/sound.js` にサウンドエンジン。**Web Audio APIでファミコン風効果音を都度合成**(矩形波/三角波オシレータ)。音声ファイルは持たない=依存・アセットゼロを維持。ブラウザの自動再生制限のため `ensureAudio()` をユーザー操作中(「会議に参加」/サウンドトグル)に呼んで初期化する。ON/OFFは `localStorage`(`tk_sound`)に永続化。効果音: ボタンクリック/退出/会議参加/正解/失敗/全クリア/カウントダウン、および**字幕に同期したテキスト送り音**(`playCaption`、沈黙「……」時は `playSilence` の不穏な単音)。字幕音は異変の有無で変えない(早期に異変を悟らせないため)。
- 異変は `ANOMALIES` 配列で宣言的に定義。各異変は `buildView()` が生成するビューモデル(参加者リスト、会議名、字幕、時計、退出ボタンのラベル等)への変換関数 `apply(v)` として実装する。**新しい異変はこの配列に追加するだけで組み込まれる。**
- 1ランで同じ異変は再出現しない(`usedAnomalies`)。異変出現率は `ANOMALY_RATE = 0.55`。
- アバターはSVG手描き。人物ごとに髪型(`hair`: short/parted/bob/buzz/long)・髪色・眉(`brow`: straight/soft/thick)・眼鏡で描き分け、耳・首・頬の影・白目とハイライト・鼻まで描き込んでいる。まばたき・口パクはCSSアニメーション。この顔パーツ(眉・耳・目の色・口・額)が後述の「描き込みにちなんだ異変」のフックになっている。

## 異変の3層構造(重要な設計判断)

1. **最初から存在する系**: 名前の漢字違い(渡辺→渡部)、会議名「日次定例」→「目次定例」、知らない参加者、映像反転、ミュート外れ、背景の人影、レコーディング表示、自分の名前が「私」、顔色、時計10:66、参加者数表示、全員「田中」、字幕逆さ、退出ボタンが「脱出」、佐藤が一度もまばたきしない(`frozenEyes`)
   - **顔の描き込みにちなんだ系**: 鈴木の眉毛が消える(`noBrows`)、高橋の耳がとがる(`pointedEars`)、渡辺の目が赤い(`eyeColor`)、佐藤が歯を見せて笑い続ける(`grin`)、田中が口紅をつけている(`lipstick`)
2. **時間経過で現れる系**: 司会の発言が不気味になる(3つ目の字幕、約10秒後から)、ミュート中なのに自分の口が動く(`selftalk`, t>0.4)、途中で知らない参加者が入室(`joiner`, t≥0.45)、全員が静止してこちらを見て字幕が「……」になる(`silence`, t≥0.55)、残り時間表示が途中から増えていく(`timerup`, 表示が0:17で折り返す。実時間は進行)
3. **じわじわ進行する系**: 佐藤の顔が肥大化(scale = 1 + 1.6·t²)、鈴木の画面が赤く染まる(opacity = 0.65·t^1.5)、高橋の背景の人影が手前に近づく(`approach`, translate/scale を t² で進行)、田中の額に第三の目がゆっくり開く(`thirdEye`, ry を t² で開眼)

進行カーブを二乗/1.5乗にしているのは意図的。線形だと序盤で気づけてしまう。前半「気のせいか?」→後半加速、が狙い。2層・3層の存在により「異変がなさそうでも最後まで疑い続ける」緊張が生まれ、異変なしの日の待ち時間が退屈にならない。**バランス調整時はこの構造を壊さないこと。**

## デプロイ

- 公開先: `taross-f/taross-f.github.io` の `apps/teirei-kaigi/`(URL: https://taross-f.github.io/apps/teirei-kaigi/)
- `vite.config.js` の `base` は `/apps/teirei-kaigi/` 設定済み。
- `npm run build` → `dist/` の中身を上記パスにコミット、またはGitHub Actionsで自動化。

## バックログ(優先度順)

1. OGP画像の追加(Xでのシェア導線。index.htmlのog:imageが未設定)
2. アバターの差し替え: SVG手描き→AI生成の実写風静止画+ループ動画。**不気味さの8割はここで決まる**
3. ~~サウンド~~ → ファミコン風効果音を実装済み(`src/sound.js`)。残: 司会の声・環境ノイズ・異変時の環境音の違和感など、より没入的な音響は今後の課題
4. 異変の難易度バランス検証(「まばたき同期」「参加者数表示」は気づきにくすぎる可能性)
5. 類似先行作の調査(Zoomホラーゲームが既にないかリリース前に確認)
6. 正解時に異変名を表示している(transition.detail)が、本番では伏せるか要検討(8番出口は教えない)
